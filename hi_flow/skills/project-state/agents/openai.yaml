interface:
  display_name: "Project State"
  short_description: "Resume and update project status"
  default_prompt: "Use $project-state to show where this hi_flow project stands and what to do next."

policy:
  allow_implicit_invocation: true
