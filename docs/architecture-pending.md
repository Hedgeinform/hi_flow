[//]: # (Architecture Pending — список архитектурных событий, фиксация которых отложена.)
[//]: # (Управляется скиллом `architecture`. Закрытие — удалением записи.)

# Architecture Pending

---

## 2026-04-29 — Open Question

**Что:** В шаблонах refactor-планов с шагами «Re-run audit» к каждому такому шагу должна прикрепляться конкретная команда (канал + grep), а каждое «Expected: ...» — указывать **где** expected проверяется. Без этого agent-failure rate в audit-валидации остаётся ~30%; с этим — ~5%.

**Контекст возникновения:** Замечание пришло после реальной refactor-сессии, в которой агент проваливал шаги Re-run audit из-за неоднозначности «куда смотреть».

**Почему отложено:** Сейчас планы пишутся через `superpowers:writing-plans` напрямую — собственного шаблона refactor-плана с audit-валидацией у нас нет. Рекомендация актуализируется только если/когда мы решим переписать планировщик под семейство hi_flow (например, в составе `hi_flow:impl-plan`, который сейчас parked per D10).

**Trigger для re-review:** появление работы над собственным шаблоном refactor-плана в hi_flow (или эквивалентным артефактом в arch-redesign output).

**Предлагаемый раздел при материализации:** скорее всего попадёт в SKILL.md соответствующего скилла как required structural element шаблона, не в ARCHITECTURE.md проекта.

**Отложено:** 1 раз
**Первое предложение:** 2026-04-29

---
